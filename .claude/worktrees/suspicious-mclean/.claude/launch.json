{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "Plugin Dev (watch)",
      "runtimeExecutable": "pnpm",
      "runtimeArgs": ["dev"],
      "port": null,
      "description": "Vite watch-mode build: outputs to dev/ directory (no HTTP server). SiYuan loads the plugin from there via make-link."
    }
  ]
}
